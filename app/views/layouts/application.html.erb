<!DOCTYPE html>
<html>
  <head>
    <title>Blazon</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  </head>

  <body>
  <div class="header">
  <%= image_tag "https://upload.wikimedia.org/wikipedia/commons/thumb/7/79/Old_Keio_University_Library_3.jpg/480px-Old_Keio_University_Library_3.jpg", width: 300 %>
  <% if user_signed_in? %>
  <%= link_to "new blazo", new_blazo_path %>
  <%= link_to "timeline", root_path%>
  <%= link_to "recomended users", users_index_path%>
  <%= link_to "profile", users_edit_path(current_user) %>
  <%= link_to "account settings", edit_user_registration_path %>
  <%= link_to "sign out", destroy_user_session_path%>
  <% end %>
  </div>
  <p class="notice", style="color: green"><%= notice %></p>
  <p class="alert", style="color: red"><%= alert %></p>
  <%= yield %>
  </body>
</html>
